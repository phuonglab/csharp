#Convert string <=> byte array
  +string => byte array 
    byte[] array = Encoding.ASCII.GetBytes(input); //input is string
  +byte array => sring
    string value = ASCIIEncoding.ASCII.GetString(array);
#Use unsafe keyword to use pointer in c#
#Use msgBox
  DialogResult result = MessageBox.Show("tittle","content",MessageBoxButtons.YesNoCancel
  , MessageBoxIcon.Question,MessageBoxOptions.RightAlign,true);
  
/* referent link */  
http://msdn.microsoft.com/en-us/library/kx37x362.aspx  // all 
http://msdn.microsoft.com/en-us/library/aa288436%28v=vs.71%29.aspx // Tutorials
http://www.dotnetperls.com/
http://diendan.congdongcviet.com/showthread.php?t=33540 // plug in for c#

#Set max characters for textBox : textBox.MaxLength
#Use SelectedIndex to set/get index for comboBox
#Compare 2 string String.Equals(port, "COM1", StringComparison.OrdinalIgnoreCase)
#File : use StreamReader 
